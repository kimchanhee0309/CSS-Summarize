# 01. 기본개념
## 1. ID와 Class의 차이
<details>
<summary>(1) 아이디(ID)</summary>

### 아이디(ID)의 정의
* HTML 문서 내에서 **유일한(Unique)** 특정 요소를 선택할 때 사용
* HTML 태그 내에 **id="이름"** 속성을 사용하여 지정
* CSS 파일에서는 아이디 이름 앞에 **샵(#)** 기호를 붙여 표현

### 문법(Syntax)
``` HTML
<h1 id="main-title">페이지 제목</h1>
```
``` CSS
#main-title {
  color: navy;
  font-size: 32px;
}
```

### 핵심 규칙(Key Rules)
**1. 페이지 내 유일성(Uniqueness)**
* 하나의 HTML 문서 안에서 같은 아이디 이름은 **단 한 번만** 등장해야 함

**2. 단일 값 허용**
* 하나의 HTML 요소는 오직 하나의 아이디만 가질 수 있음

**3. 명명 규칙(Naming Convention)**
* 대소문자를 구별함 (**#Header**와 **#header**는 다름)
* 공백을 포함할 수 없음
* 숫자로 시작할 수 없음

**4. 언제 사용? 이 페이지에 딱 하나밖에 없는 요소**를 디자인할 때 주료 사용함

</details>

<details>
  <summary>(2) 클래스(class)</summary>

  ### 클래스(Class)의 정의
  * 특정 스타일 규칙을 **여러 HTML 요소**에 동일하게 적용하기 위해 사용하는 식별자
  * HTML 태그 내에 **class="이름"** 속성을 사용하여 지정함
  * CSS 파일에서는 클래스 이름 앞에 **마침표(.)** 기호를 붙여 표현

  ### 문법
  * 여러 요소에 동일한 클래스 이름을 부여하고, CSS에서 한 번에 스타일을 제어하는 방식
  ``` HTML
<h2 class="highlight">강조된 제목</h2>
<p class="highlight">강조된 본문 내용입니다.</p>
```
``` CSS
/* .기호를 사용하여 해당 클래스를 가진 모든 요소를 선택 */
.highlight {
  background-color: yellow;
  font-weight: bold;
```
 
  ### 핵심 특징
  **1. 재사용성(Reusability)**
  * 한 페이지 내에서 동일한 클래스 이름을 횟수 제한 없이 사용할 수 있음
  * 이를 통해 버튼, 카드, UI, 리스트 아이템 등 **반복되는 디자인 요소를 효율적으로 관리함**
  
  **2. 다중 클래스 허용(Multiple Classes)**
  * 하나의 HTML 요소가 **두 개 이상의 클래스를 가질 수 있음**
    * **문법** : 공백(Space)으로 구분하여 나열함
    * **HTML** : `<button class="btn btn-primary">버튼</button>`
    * **작동 원리** : `btn` 클래스의 스타일(기본 모양)과 `btn-primary` 클래스의 스타일(파란색 배경)이 동시에 적용됨

  ### 명명 규칙
  * **1. 케밤 케이스(Kebab-case)**
    * 단어와 단어 사이를 하이픈(-)으로 연결하는 방식
    * ex) `menu-item`, `profile-image`
      
  * **2. 첫 글자 제한**
    * 반드시 **문자**로 시작해야 함
    * 숫자/특수문자로 시작하는 클래스 이름은 브라우저가 인식하지 못하거나 오류를 발생시킴
      * 잘못된 예: `.2nd-item`
      * 올바른 예: `.item-2`
  * **3. 대소문자 구분**
    * `.Box`와 `.box`는 서로 다른 클래스로 인식됨   
  
</details>

<details>
  <summary>(3) ID vs Class 비교 요약</summary>

  |**구분**|**아이디(ID)**|**클래스(Class)**|
  |-----|-----|-----|
  |**기호**|`#`(샵)|`.`(마침표)|
  |**사용 횟수**|페이지 당 1회(유일성)|제한 없음(반복 가능)|
  |**속성 값**|하나의 값만 가능|공백으로 구분하여 여러 개 가능|
  |**주 용도**|레이아웃의 큰 틀, 유일한 요소|버튼, 텍스트 스타일 등 반복 요소|
</details>

___

## 2. CSS 파일(External Stylesheet) 분리

___

## 3. 다양한 색상 단위(RGB, RGBA, HEX 등)★
