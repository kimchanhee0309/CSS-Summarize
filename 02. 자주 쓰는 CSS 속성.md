## 1. 텍스트 스타일링 총정리⭐️
<details>
  <summary>(1) 글꼴 지정</summary>

  * **`font-family` 사용**
  * 사용할 글꼴의 우선순위 목록을 지정함
    * **1. Fallback 시스템**
      * 나열된 순서대로 사용자의 컴퓨터에 해당 폰트가 있는지 확인하고, 없으면 다음 폰트로 넘어감
        
    * **2. Generic Family**
      * 목록의 맨 마지막에는 반드시 폰트 계열(Generic name)을 적어 주어야 함
      * ex) `sans-serif`, `serif`, `monospace`

     ```CSS
     /* 문법: font-family: "1순위 폰트", "2순위 폰트", 계열;
     p {
       /* 1. Pretendard가 있으면 사용 */
       /* 2. 없으면 Malgun Gothic 사용 */
       /* 3. 다 없으면 시스템 기본 고딕체(sans-serif) 사용 */
       font-family: "Pretendard", "Malgun Gothic", sans-serif;
     ```     
</details>

<details>
  <summary>(2) 굵기 조절</summary>

  * **`font-weight` 사용**
  * 글자의 두께를 결정
    * **1. `normal`(기본값), `bold`(굵게)**
    * **2. 숫자 값(100 ~ 900)**
      * 폰트가 지원하는 경우 더 세밀한 두께 조절이 가능함
        * 400 : normal과 동일
        * 700 : bold와 동일

    ```CSS
    h1 {
      font-weight: 700;
    }
    p {
      font-weight: 400;
    }
    .light-text {
      font-weight: 300;
    }  
    ```
</details>

<details>
  <summary>(3) 정렬</summary>

  * **`text-align` 사용**
  * 블록 내부의 인라인 콘텐츠(텍스트, 이미지 등)를 정렬함
    * `left`: 왼쪽 정렬(기본값)
    * `right`: 오른쪽 정렬
    * `center`: 가운데 정렬
    * `justify`: 양쪽 정렬(문단 양 끝을 맞추기 위해 자간이 자동으로 조절됨)
   
    ```CSS
    .title {
      text-align: center;
    }
    .article-body {
      text-align: justify;
    }
    ```
</details>

<details>
  <summary>(4) 줄 간격(중요)</summary>

  * **`line-height` 사용**
  * 행과 행 사이의 높이를 지정
  * **가독성**에 가장 큰 영향을 미치는 속성
    * **1. 단위 없는 숫자 권장** : `px`나 `%`보다 단위 없는 배수 값(예:1.5)을 사용하는 것이 상속 문제 없이 안전함
    * **2. 수직 중앙 정렬 팁** : 한 줄짜리 텍스트의 경우, `height`와 `line-height` 값을 똑같이 주면 수직 중앙에 배치됨

    ```CSS
    p {
      font-size: 16px;
      line-height: 1.5; /* 실제 줄 높이: 16px * 1.5 = 24px */
    }

    /* 버튼 텍스트 수직 중앙 정렬 예시 */
    .btn {
      height: 50px;
      line-height: 50px;
    }
    ```
</details>

<details>
  <summary>(5) 텍스트 꾸밈</summary>

  * **`text-decoration` 사용**
  * 주로 링크(`<a>`)의 밑줄을 제어하거나 취소선을 그을 때 사용
    * `none` : 꾸밈 없음(링크의 기본 밑줄을 제거할 때 필수)
    * `underline` : 밑줄
    * `line-through` : 취소선(할 일 완료 표시, 할인 전 가격 등)
   
    ```CSS
    a {
      text-decoration: none;
      color: inherit; /* 부모 색상 따르기 */
    }

    .finished-task {
      text-decoration: line-through;
      color: #999;
    ``` 
</details>

___

## 2. 배경 이미지(background-image)
<details>
  <summary>(1) 기본 문법</summary>

  * **`url()` 함수 사용**
    * 경로(Path)
      * **상대 경로** : 현재 CSS 파일 위치를 기준으로 이미지를 찾음
      * **절대 경로(URL)** : 웹상의 이미지 주소를 직접 입력

    ```CSS
    .hero-sercion {
      width: 100%;
      height: 500px;
      background-image: url("...");
    ```
</details>

<details>
  <summary>(2) 반복 제어</summary>

  * **`background-repeat` 사용**
  * 이미지 크기가 요소보다 작을 때 제어하는 용도로 사용
    * `repeat` : 가로, 세로로 계속 반복(기본값)
    * `no-repeat` : 반복하지 않고 한 번만 표시(가장 많이 사용)
    * `repeat-x` : 가로 방향으로만 반복
    * `repeat-y` : 세로 방향으로만 반복

    ```CSS
    .icon-box {
      width : 300px;
      height: 300px;
      background-image: url("...");
      background-repeat: no-repeat;
    ```
</details>

<details>
  <summary>(3) 위치 조정</summary>

  * **`background-position` 사용**
  * 이미지가 요소 내부의 어디에 위치할 지 결정
  * 가로축(x)과 세로축(y) 값을 순서대로 입력
    * **키워드** : `top`, `bottom`, `left`, `right`, `center`
    * **단위 값** : `px`, `%` 등 정밀한 수치

    ```CSS
    .profile {
      background-position: center center;
    }

    .banner {
      background-position: right 20px;
    ```
</details>

<details>
  <summary>(4) 크기 조절⭐️</summary>

  * **`background-size` 사용**
  * 이미지의 크기를 제어, 반응형 웹에서 매우 중요한 속성
    * `auto`: 이미지의 원본 크기 유지(기본값)
    * **`cover`**
      * **(가장 중요)** 요소의 영역을 **빈틈없이 가득 채움**
      * 이미지 비율을 유지하며, 영역보다 이미지가 크면 넘치는 부분은 잘림
      * 배경으로 꽉 찬 이미지를 넣을 때 필수
    * `contain`
      * 이미지의 **모든 부분이 보이도록** 크기를 조절함
      * 비율은 유지되지만, 요소보다 이미지가 작으면 빈 공간(여백)이 생길 수 있음
    * `px`/`%`: 특정 크기로 강제 지정함
   
    ```CSS
    .full-screen-bg {
      width: 100%;
      height: 100vh;
      background-image: url("...");

      background-size: cover;
      background-position: cenger;
    }

    .logo-container {
      width: 200px;
      height: 100px;
      background-image: url("...");

      background-size: contain;
      backgroind-repeat: no-repeat;
    ```   
</details>

___

## 3. 그라디언트(linear-gradient)
* CSS에서 색상이 아닌 이미지로 취급되기 때문에, `background-image` 속성을 사용하여 적용해야 함

<details>
  <summary>(1) 기본 문법(Syntax)</summary>

  * `linear-gradient()` 함수를 사용하며, 진행 방향과 색상 정지점(Color Stop)을 순서대로 입력함
    * 기본형: `background-image: linear-gradient(방향, 시작색상, 종료색상);`
    * 특징: 색상은 콤마(,)로 구분하여 무제한으로 추가할 수 있음
</details>

<details>
  <summary>(2) 방향 설정(Direction)</summary>

  * 그라디언트가 어느 쪽으로 흐를지 결정함
    * **키워드 사용** : `to` 다음에 목적지를 적음
      * `to bottom`: 위에서 아래로(기본값, 생략 가능)
      * `to right`: 왼쪽에서 오른쪽으로
      * `to bottom right`: 왼쪽 위에서 오른쪽 아래 대각선으로
     
    * **각도(Degree) 사용** : 시계 방향 각도를 정밀하게 지정
      * `0deg`: 아래에서 위로 (`to top`)
      * `90deg`: 왼쪽에서 오른쪽으로 (`to right`)
      * `180deg`: 위에서 아래로 (`to bottom`)
      * `45deg`: 대각선
     
    ```CSS
    .box1 {
      background-image: linear-gradient(blue, red);
    }

    .box2 {
      background-image: linear-gradient(to right, blue, red);
    }

    .box3 {
      background-image: linear-gradient(45deg, blue, red);
    }
    ```
</details>

<details>
  <summary>(3) 색상 정지점(Color Stops)</summary>

  * 색상이 바뀌는 지점을 %나 px로 지정할 수 있음
  * 값을 지정하지 않으면 균등하게 배분됨
    * **위치 지정**: 색상 뒤에 공백을 두고 위치값을 적음
    * **활용**: 특정 지점까지 색을 유지하거나, 색을 급격하게 바꿀 때 사용
   
    ```CSS
    .rainbow {
      /* 3가지 색상 균등 분할 */
      background-image: linear-gradient(to right, red, yellow, green);
    }

    .custom-stop {
      /* 빨강이 30% 지점까지 차지하고, 그 뒤부터 파랑으로 변함 */
      background-image: linear-gradient(to right, red 30%, blue);  
    ```
</details>

<details>
  <summary>(4) 실무 활용: 이미지 위에 딤드(Dimmed) 처리</summary>

  * 텍스트 가독성을 위해 배경 이미지 위에 반투명한 검은색 그라디언트를 겹칠 때 가장 많이 사용
  * `background-image`는 콤마로 구분하여 **여러 겹의 이미지**를 쌓을 수  ㅣㅆ음
    * **순서**: 먼저 작은 것이 가장 위(Top)에 올라옴
    * **공식**: `linear-gradient(...)`, `url(...)`
    ```CSS
    .hero-overlay {
      /* 1. 위에 까만 반투명 그라디언트 (rgba 사용 필수) */
      /* 2. 아래에 실제 배경 이미지 */
      background-image:
        linear-gradient (rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),
        url("...);

      background-size: cover;
      color: white;
    }
    ```   
</details>

___

## 4. 그림자(box-shadow)
<details>
  <summary>(1) 기본 문법(Syntax)</summary>

  * 최대 5개의 값과 1개의 키워드를 조합하여 사용함. 순서가 중요
  * 작성 순서 : `offset-x offset-y blur-radius spread-radius color`
    * **1. offset-x(수평거리)** : 그림자의 가로 위치
      * 양수(+): 요소의 **오른쪽**에 그림자가 생김
      * 음수(): 요소의 **왼쪽**에 그림자가 생김
    * **2. offset-y(수직거리)** : 그림자의 세로 위치
      * 양수(+): 요소의 **아래쪽**에 그림자가 생김
      * 음수(): 요소의 **위쪽**에 그림자가 생김
    * **3. blur-radius(흐림 정도)** : *[선택 사항]* 그림자의 가장자리를 얼마나 흐리게 만들지 결정
      * `0`: 가장자리가 칼처럼 날카로운 선명한 그림자가 됨
      * 값이 클수록 그림자가 넓게 퍼지며 흐려짐(음수 사용 불가)
    * **4. spread-radius(확산 정도)** : *[선택 사항]* 그림자의 전체 크기를 조절
      * 양수: 그림자가 요소보다 더 크게 확산됨
      * 음수: 그림자가 요소보다 작게 축소됨
    * **5. color(색상)** : *[선택 사항]* 그림자의 색상
      * 지정하지 않으면 브라우저 기본값(보통 검은색)이 적용되지만, **`rgba`를 사용하여 투명도를 조절**하는 것이 자연스러움

    ```CSS
    /* 오른쪽으로 10px, 아래로 10px, 흐림 5px, 검은색 */
    .basic-shadow {
      box-shadow: 10px 10px 5px black;
    }
    ```
</details>

<details>
  <summary>(2) 내부 그림자</summary>

  * 기본적으로 그림자는 요소의 바깐쪽(Outset)으로 확산됨
  * **`inset`키워드**를 추가하면 그림자가 요소의 **안쪽**으로 생겨, 마치 요소가 눌려 있거나 움푹 파인 듯한 효과를 줌

  ```CSS
  .inner-shadow {
    box-shadow: inset 5px 5px 10px rgba(0,0,0,0.5);
  ```
</details>

<details>
  <summary>(3) 다중 그림자(Multiple Shadows)</summary>

  * 콤마(,)를 사용하여 하나의 요소에 여러 개의 그림자를 동시에 적용할 수 있음
  * 이를 통해 더 정교하고 입체적인 조명 효과를 구현함

  ```CSS
  .complex-shadow {
    /* 첫 번째: 위쪽 하이라이트 (흰색) */
    /* 두 번째: 아래쪽 그림자 (검은색) */
    box-shadow:
      -5px -5px 10px rgba(255, 255, 255, 0.7),
      5px 5px 10px rgba(0, 0, 0, 0.2);
}
  ```
</details>

___

## 5. 불투명도(opacity)⭐️
<details>
  <summary>(1) 기본 문법(Syntax)</summary>

  * 0.0~1.0 사이의 숫자를 사용하여 불투명도를 조절함
    * 값의 범위
      * `0`(0,0): **완전 투명(Invisible)**, 화면에서 보이지 않지만, 자리는 그대로 차지함
      * `0.5`: **50% 반투명**
      * `1`(1.0): **완전 불투명(Opaque)**, 기본값
     
    ```CSS
    .ghost-box {
      background-color: blue;
      opacity: 0.5; 
    ``` 
</details>

<details>
  <summary>(2) 핵심 차이점: Opacity vs RGBA</summary>

  * 배경색만 투명하게 하고 싶을 때 `opacity`를 쓰면 안 되는 이유
    * **opacity 속성**
      * **동작**: 요소 자신뿐만 아니라, **그 안에 포함된 모든 자식 요소(텍스트, 이미지 등)까지** 함께 투명해짐
      * **상속**: 투명도가 자식에세 상속되는 것처럼 동작함. 자식 요소에서 `opacity: 1`을 다시 줘도 부모의 투명도를 벗어날 수 없음
    * **RGBA(Alpha Channel)**
      * **동작**: 딱 그  **색상(배경색이나 글자색)만** 투명해짐
      * **영향**: 자식 요소(텍스트 등)에는 전혀 영향을 주지 않고 선명하게 유지됨
        
    |**구분**|**opacity: 0.5**|**background-color: rgba(0,0,0,0.5)**|
    |-----|-----|-----|
    |**적용 대상**|요소 전체(배경+내용물+자식 요소)|지정한 속성(배경색) 하나만|
    |**자식 요소**|자식 요소도 같이 흐려짐|자식 요소는 선명하게 유지됨|
    |**사용 목적**|요소 전체를 흐리게 할 때(페이드 효과 등)|배경만 은은하게 깔고 글씨는 잘 보이게 할 때| 
</details>

<details>
  <summary>(3) 시각적 상태 비교(Visibility Context)</summary>

  * **`opacity: 0'**
    * 눈에만 안 보임
    * **공간 차지함**(레이아웃에 영향 줌)
    * **클릭 가능함**(이벤트 작동)
   
  * **`visibility: hidden`**
    * 눈에 안 보임
    * **공간 차지함**
    * **클릭 불가능**
  * **`display: none`**
    * 아예 렌더링되지 않음
    * **공간 차지하지 않음**(레이아웃에서 사라짐)
    * **클릭 불가능** 
</details>
