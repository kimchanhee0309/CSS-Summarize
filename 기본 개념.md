# 01. 기본개념
## 1. ID와 Class의 차이
<details>
<summary>(1) 아이디(ID)</summary>

### 아이디(ID)의 정의
* HTML 문서 내에서 **유일한(Unique)** 특정 요소를 선택할 때 사용
* HTML 태그 내에 **id="이름"** 속성을 사용하여 지정
* CSS 파일에서는 아이디 이름 앞에 **샵(#)** 기호를 붙여 표현

### 문법(Syntax)
``` HTML
<h1 id="main-title">페이지 제목</h1>
```
``` CSS
#main-title {
  color: navy;
  font-size: 32px;
}
```

### 핵심 규칙(Key Rules)
**1. 페이지 내 유일성(Uniqueness)**
* 하나의 HTML 문서 안에서 같은 아이디 이름은 **단 한 번만** 등장해야 함

**2. 단일 값 허용**
* 하나의 HTML 요소는 오직 하나의 아이디만 가질 수 있음

**3. 명명 규칙(Naming Convention)**
* 대소문자를 구별함 (**#Header**와 **#header**는 다름)
* 공백을 포함할 수 없음
* 숫자로 시작할 수 없음

**4. 언제 사용? 이 페이지에 딱 하나밖에 없는 요소**를 디자인할 때 주료 사용함

</details>

___

## 2. CSS 파일(External Stylesheet) 분리

___

## 3. 다양한 색상 단위(RGB, RGBA, HEX 등)★
